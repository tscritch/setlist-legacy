(function() {

  'use strict';

  angular.module('setlist').config(function ($httpProvider) {
    // $httpProvider.defaults.headers.common.Autherization = 'Basic app_id'
    // $httpProvider.defaults.headers.common['Access-Control-Allow-Headers'] = 'Accept, Authorization, Content-Type, If-Match, If-Modified-Since, If-None-Match, If-Unmodified-Since, Origin, X-Requested-With';
    // $httpProvider.defaults.headers.common['Access-Control-Allow-Methods'] = 'GET, POST, PATCH, PUT, DELETE, OPTIONS';
    // $httpProvider.defaults.headers.common['Access-Control-Allow-Origin'] = '*';
  });

})();
